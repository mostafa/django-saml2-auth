# Shared test + Coveralls jobs. Called from ci.yml (PR / main) and release.yml (tags).
name: Reusable test suite

on:
  workflow_call:

permissions: {}

jobs:
  test:
    name: Test Django ${{ matrix.versions.djangoSeries }} (Python ${{ matrix.versions.pythonVersion }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        versions:
          - { "djangoSeries": "5.2", "djangoConstraint": ">=5.2.8,<5.3", "pythonVersion": "3.10" }
          - { "djangoSeries": "5.2", "djangoConstraint": ">=5.2.8,<5.3", "pythonVersion": "3.11" }
          - { "djangoSeries": "5.2", "djangoConstraint": ">=5.2.8,<5.3", "pythonVersion": "3.12" }
          - { "djangoSeries": "5.2", "djangoConstraint": ">=5.2.8,<5.3", "pythonVersion": "3.13" }
          - { "djangoSeries": "5.2", "djangoConstraint": ">=5.2.8,<5.3", "pythonVersion": "3.14" }
          - { "djangoSeries": "6.0", "djangoConstraint": ">=6.0,<6.1", "pythonVersion": "3.12" }
          - { "djangoSeries": "6.0", "djangoConstraint": ">=6.0,<6.1", "pythonVersion": "3.13" }
          - { "djangoSeries": "6.0", "djangoConstraint": ">=6.0,<6.1", "pythonVersion": "3.14" }
    permissions:
      id-token: write # reserved for OIDC-capable coverage or signing steps
      contents: read # checkout and read coverage output
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Python 🐍
        uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405 # v6.2.0
        with:
          python-version: ${{ matrix.versions.pythonVersion }}
      - name: Install uv
        uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b # v8.1.0
        with:
          enable-cache: false
      - name: Install system libraries 📦
        run: |
          sudo apt-get update
          sudo apt-get install -y xmlsec1 libxml2-dev libxslt1-dev
      - name: Install dependencies 📦
        env:
          DJANGO_CONSTRAINT: ${{ matrix.versions.djangoConstraint }}
        run: |
          uv sync
          # Install matrix Django into the job venv only. `uv add` re-resolves the whole project
          # for all `requires-python` versions; Django 6.x needs Python >=3.12 and fails that
          # universal resolution while the project still supports 3.10/3.11.
          # Semver range picks the latest patch in each supported minor line on every CI run.
          uv pip install "Django${DJANGO_CONSTRAINT}"
          uv run python -c "import django; print(f'Resolved Django {django.get_version()}')"
      - name: Check types, syntax and duckstrings 🦆
        run: |
          uv run python -m mypy --explicit-package-bases .
          uv run python -m ruff check .
      - name: Test Django ${{ matrix.versions.djangoSeries }} with coverage 🧪
        run: |
          uv run coverage run --source=django_saml2_auth -m pytest . && uv run coverage lcov -o coverage.lcov
      - name: Submit coverage report to Coveralls 📈
        uses: coverallsapp/github-action@648a8eb78e6d50909eff900e4ec85cab4524a45b # v2.3.6
        with:
          # github.token avoids secrets: inherit on callers (zizmor: secrets-inherit).
          github-token: ${{ github.token }}
          path-to-lcov: ./coverage.lcov
          # Object matrix has no splat in GHA expressions; join(matrix.versions.*, …) fails evaluation.
          flag-name: run-${{ matrix.versions.djangoSeries }}-${{ matrix.versions.pythonVersion }}
          parallel: true
  finish:
    name: Coveralls parallel finished
    needs: test
    if: ${{ always() }}
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Coveralls Finished
        uses: coverallsapp/github-action@648a8eb78e6d50909eff900e4ec85cab4524a45b # v2.3.6
        with:
          parallel-finished: true
